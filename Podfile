# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VK Market' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'SwiftLint'
  pod 'Alamofire'
  pod 'SwiftyVK'

  # Pods for VK Market

  target 'VK MarketTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VK MarketUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
